
const Questions = () => {
    const questions = [
        "sup?",
        "paper or plastic?"
    ];

    return (
        <div className='question-text'>{questions[0]}</div>
    );
};

export default Questions;
